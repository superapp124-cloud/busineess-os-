# Echo Capability

The Echo capability proves that the platform can predictably and deterministically map inputs to outputs across the Execution Engine without unwanted mutations.

## Purpose
- Prove **Deterministic Execution**.
- Validate schema parsing for deep object payloads.
