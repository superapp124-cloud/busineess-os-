# Hello World Capability

The Hello World capability serves as the absolute baseline for the CHATR Intent OS platform. It proves that the capability lifecycle (Scaffold → Validate → Test) functions correctly without introducing business logic complexity.

## Purpose
- Prove **Contract Validity** (Minimum Kernel Version, Publisher, Actions).
- Serve as the simplest possible target for `chatr test --conformance`.
