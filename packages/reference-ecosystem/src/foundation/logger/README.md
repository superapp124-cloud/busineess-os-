# Logger Capability

The Logger capability proves that capabilities can correctly hook into the platform's Observability layer (Event Mesh) to emit structured events without crashing the execution context.

## Purpose
- Prove **Event Emission** and observability contracts.
