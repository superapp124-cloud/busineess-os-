# Phase 2C Completion Report — Lovable Dependency Audit

**Branch:** `chore/lovable-exit`
**Cumulative contract tests:** 212/212 passing
**Active Lovable runtime references in codebase:** 0

---

## Audit Scope

| Domain | Files Scanned | Lovable Runtime Refs |
|---|---|---|
| Local migrations | 81 `.sql` files | **0** |
| Edge Function secrets | 58 unique secrets across all functions | **0** |
| Deployment binding points | `vercel.json`, `.env.example`, `capacitor.config.ts`, `supabase/config.toml`, `client.ts`, `android/strings.xml` | **0 Lovable refs** |
| Active Edge Functions | All of `supabase/functions/` | **0** |
| MCP server | `chatr-mcp-server` | **0** |
| Webhooks | `bot-webhook`, `business-ivr-webhook`, `connector-hub` | **0** |
| `backups/` (pre-Phase-2B historical snapshots) | Contains Lovable refs | Expected — NOT deployed code |

---

## Action 1 — `src/integrations/supabase/client.ts` (DONE)

**Change made:**
- Removed the Lovable-generated `// This file is automatically generated. Do not edit it directly.` comment.
- Added CHATR ownership header documenting the project ref, anon key policy, and V2 portability path.
- Added `getEnv('VITE_SUPABASE_URL')` and `getEnv('VITE_SUPABASE_ANON_KEY')` as primary reads, with the production hard-coded values as fallbacks.
- **No change to the Supabase project, URL, key, or any auth/data behavior.**

**Why the anon key in source is safe:**
The Supabase anon key is public by design — it is subject to Row Level Security, not secrecy. The existing resilient storage wrapper (added in Phase 1, commit `ec4d79b9`) is fully preserved.

**V2 portability enabled:**
Setting `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` in any deployment environment (Vercel, CI, `.env.local`) will now redirect the client without a code change.

---

## Action 2 — `android/app/src/main/res/values/strings.xml` (DOCUMENTED — NO CODE CHANGE)

**Exact lines found:**
```xml
<!-- Supabase config for native overlay caller ID lookup -->
<string name="supabase_url">https://sbayuqgomlflmxgicplz.supabase.co</string>
<string name="supabase_anon_key">eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...</string>
```

**Classification:** Supabase project binding (anon key + URL). **Not a Lovable dependency.**
**Purpose:** Native Android overlay uses this for caller ID lookup via the Supabase REST API.
**V1 decision:** No change. Value is correct; anon key is public.

**V2 migration point (documented here):**
When project `sbayuqgomlflmxgicplz` is transferred or a new project is introduced, `strings.xml` must be updated in the same Android release as `client.ts` and the deployment environment. This is a one-time coordinated update, not a migration.

---

## Action 3 — Supabase Dashboard Ownership (VERIFICATION REQUIRED — NOT A TECHNICAL CHANGE)

### What to verify
1. Open: `https://supabase.com/dashboard/project/sbayuqgomlflmxgicplz`
2. **Settings → General** — what account/email owns this project?
3. **Settings → Team** — which accounts have Owner or Admin access?
4. **Organization** (sidebar) — is this under a personal org or a named organization?

### The two outcomes

| State | Meaning | Next step |
|---|---|---|
| Project is under **your own** Supabase account | CHATR has full independent production ownership now. | Proceed to Phase 3 directly. |
| Project is under a **Lovable-controlled** organization | Lovable retains billing/dashboard control of production. | File a Supabase project **transfer** request. This moves org/billing ownership — it does NOT move data, schema, auth, or secrets. Zero downtime. |

In neither case do we migrate the database. All user data, auth sessions, storage objects, Edge Functions, and secrets remain exactly where they are.

---

## Secrets Inventory — Corrected Four-Way Classification

The Phase 2C audit found **58 unique secrets** referenced in Edge Functions.
"Observed in code" does NOT mean "verified in production". Each secret's status must be independently established for Phase 3 readiness.

### Classification definitions

| Status | Meaning |
|---|---|
| `OBSERVED_IN_CODE` | Secret name found in Edge Function source; presence in production vault not independently verified |
| `OPERATIONALLY_VERIFIED` | Function confirmed working in production, implying secret is set |
| `REQUIRED_NOT_VERIFIED` | Code path exists but function not confirmed active in production |
| `VALUE_UNKNOWN` | Secret referenced; value structure never exposed (e.g., service account JSON blob) |

### Inventory (all currently `OBSERVED_IN_CODE` pending Phase 3 verification)

| Group | Count | Secrets |
|---|---|---|
| **Supabase builtins** (auto-injected) | 5 | `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_JWT_SECRET`, `SUPABASE_DB_URL` |
| **AI Providers** | 10 | `GEMINI_API_KEY`, `GOOGLE_AI_API_KEY`, `GROQ_API_KEY`, `OPENAI_API_KEY`, `OPENROUTER_API_KEY`, `GEMINI_MODEL`, `GROQ_MODEL`, `OPENAI_MODEL`, `OPENROUTER_MODEL`, `AI_SEARCH_PRIMARY_PROVIDER` |
| **Firebase / FCM** | 6 | `FIREBASE_API_KEY`, `FIREBASE_PROJECT_ID`, `FIREBASE_PROJECT_IDS`, `FIREBASE_SERVER_KEY`, `FIREBASE_SERVICE_ACCOUNT`, `VITE_FIREBASE_PROJECT_ID` |
| **Search providers** | 7 | `BRAVE_API_KEY`, `BRAVE_SEARCH_API_KEY`, `BING_SEARCH_KEY`, `GOOGLE_SEARCH_API_KEY`, `GOOGLE_SEARCH_CX_ID`, `SERPER_API_KEY`, `FIRECRAWL_API_KEY` |
| **Communications** | 7 | `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_PHONE_NUMBER`, `TWILIO_VOICE_CALLBACK_URL`, `RESEND_API_KEY`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_WEBHOOK_VERIFY_TOKEN` |
| **WebRTC / Realtime** | 6 | `TURN_URLS`, `TURN_USERNAME`, `TURN_CREDENTIAL`, `TURN_ICE_SERVERS_JSON`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` |
| **Payments / Maps / Jobs** | 4 | `RAZORPAY_KEY_ID`, `GOOGLE_MAPS_API_KEY`, `ADZUNA_API_KEY`, `ADZUNA_APP_ID` |
| **Platform / Security** | 13 | `CHATR_CRON_SECRET`, `CHATR_MACHINE_TOKEN`, `DIGEST_CRON_SECRET`, `JWT_SECRET`, `JWT_SIGNING_SECRET`, `INTERNAL_FUNCTION_TOKEN`, `DPDP_UPLOADER_SALT`, `EDGE_RATE_LIMIT_DISABLED`, `METERED_API_KEY`, `SITE_URL`, `ALLOWED_ORIGINS`, `CHATR_ALLOWED_ORIGINS`, `ALLOW_VERCEL_PREVIEW_ORIGINS` |
| **Lovable secrets** | **0** | _(none)_ |

The Phase 3 readiness task is to walk through this table and assign each secret a verified status — either by confirming it in the Supabase vault, observing the dependent function running in production, or by explicit owner confirmation.

---

## Phase 2C Completion Checklist

| Criterion | Status |
|---|---|
| `client.ts` Lovable-generated comment removed | ✅ |
| `client.ts` CHATR ownership header written | ✅ |
| `client.ts` env-var fallback added (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) | ✅ |
| `android/strings.xml` exact binding lines inspected and documented | ✅ |
| Android binding classified as V2 migration point (no V1 code change) | ✅ |
| Supabase dashboard ownership verification instructions written | ✅ |
| Secrets inventory corrected with 4-way classification framework | ✅ |
| Zero active Lovable runtime references in codebase confirmed | ✅ |
| Phase 2C report committed to `chore/lovable-exit` | ⏳ Pending commit |

---

## Phase 3 Entry Condition

Phase 3 (Production Readiness Review) begins when the Supabase dashboard ownership check is complete.

**Phase 3 scope — does NOT include a database migration:**
- Confirm CI/CD and deployment pipeline do not depend on Lovable infrastructure.
- Confirm Android build pipeline does not depend on Lovable infrastructure.
- Perform targeted secret verification (OPERATIONALLY_VERIFIED vs REQUIRED_NOT_VERIFIED).
- If project is under personal account → declare CHATR Lovable-independent.
- If project is under Lovable org → file Supabase project transfer request → verify transfer → declare CHATR Lovable-independent.

**The central Phase 3 question:**
> Can CHATR be built, deployed, and operated independently of Lovable while continuing to use the exact same production Supabase project `sbayuqgomlflmxgicplz`?

Based on the Phase 2C audit, the answer is already effectively yes — subject to the dashboard ownership confirmation.
