{
  "abi": "chatr.capability_contracts.v0_9_rc",
  "contract_version": "1.0.0",
  "description": "Universal Capability Contracts — CHATR Kernel v0.9 RC. Each contract defines input schema, output schema, risk, approval, context requirements, strategy support, expected observations, and verification rules for one universal capability. No contract may encode an industry, domain, or provider.",
  "generated_at": "2026-07-15T00:00:00Z",
  "contracts": {
    "DISCOVER": {
      "contract_version": "1.0.0",
      "purpose": "Find possible providers, merchants, resources, records, or options.",
      "required_inputs": ["context_ref", "entity_graph_ref"],
      "outputs": ["candidate_options"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["context_ref", "entity_graph_ref", "permissions"],
      "strategy_support": ["fastest", "cheapest", "highest_rated", "most_trusted", "local_first", "privacy_first", "energy_efficient", "offline_first", "user_preferred", "policy_required"],
      "expected_observations": ["candidate_options.available"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "FETCH": {
      "contract_version": "1.0.0",
      "purpose": "Retrieve a known object or state.",
      "required_inputs": ["context_ref", "entity_ref"],
      "outputs": ["object_state"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["context_ref"],
      "strategy_support": ["fastest", "most_trusted", "offline_first", "privacy_first"],
      "expected_observations": ["object_state.loaded"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "COMPARE": {
      "contract_version": "1.0.0",
      "purpose": "Rank or compare options.",
      "required_inputs": ["candidate_options", "constraints"],
      "outputs": ["ranked_options"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["preferences", "policy_refs"],
      "strategy_support": ["fastest", "cheapest", "highest_rated", "most_trusted", "user_preferred", "policy_required"],
      "expected_observations": ["ranked_options.ready"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "SELECT": {
      "contract_version": "1.0.0",
      "purpose": "Capture user or system selection.",
      "required_inputs": ["ranked_options", "selection_criteria"],
      "outputs": ["selected_option"],
      "risk": "medium",
      "approval": "user_when_ambiguous",
      "context_requirements": ["preferences", "policy_refs"],
      "strategy_support": ["user_preferred", "most_trusted", "highest_rated", "policy_required"],
      "expected_observations": ["selection.recorded"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "AUTHENTICATE": {
      "contract_version": "1.0.0",
      "purpose": "Establish identity, session, or authorization.",
      "required_inputs": ["identity_ref", "auth_requirement"],
      "outputs": ["session_ref"],
      "risk": "high",
      "approval": "user",
      "context_requirements": ["identity", "permissions"],
      "strategy_support": ["most_trusted", "privacy_first", "policy_required"],
      "expected_observations": ["identity.verified"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "AUTHORIZE": {
      "contract_version": "1.0.0",
      "purpose": "Obtain explicit user or policy approval.",
      "required_inputs": ["action_summary", "risk", "policy_refs"],
      "outputs": ["approval_receipt"],
      "risk": "high",
      "approval": "explicit_authorization",
      "context_requirements": ["identity", "policy_refs"],
      "strategy_support": ["most_trusted", "policy_required"],
      "expected_observations": ["authorization.granted"],
      "verification_rules": { "required": true, "evidence": ["receipt_or_observation"] }
    },
    "COLLECT_INPUT": {
      "contract_version": "1.0.0",
      "purpose": "Gather structured user or provider fields through schema.",
      "required_inputs": ["input_schema", "current_context"],
      "outputs": ["structured_fields"],
      "risk": "medium",
      "approval": "user",
      "context_requirements": ["context_ref"],
      "strategy_support": ["fastest", "user_preferred"],
      "expected_observations": ["input.completed"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "PAY": {
      "contract_version": "1.0.0",
      "purpose": "Move money or initiate payment.",
      "required_inputs": ["amount_or_payment_authorization", "payment_instrument_ref", "policy_refs"],
      "outputs": ["payment_receipt"],
      "risk": "high",
      "approval": "explicit_authorization",
      "context_requirements": ["identity", "wallet", "policy_refs"],
      "strategy_support": ["cheapest", "most_trusted", "fastest", "policy_required"],
      "expected_observations": ["payment.receipt.created"],
      "verification_rules": { "required": true, "evidence": ["receipt_or_observation"] }
    },
    "TRANSFER": {
      "contract_version": "1.0.0",
      "purpose": "Move value between accounts or stores.",
      "required_inputs": ["value_ref", "source_ref", "target_ref", "policy_refs"],
      "outputs": ["transfer_receipt"],
      "risk": "high",
      "approval": "explicit_authorization",
      "context_requirements": ["identity", "wallet", "policy_refs"],
      "strategy_support": ["cheapest", "most_trusted", "fastest", "policy_required"],
      "expected_observations": ["transfer.receipt.created"],
      "verification_rules": { "required": true, "evidence": ["receipt_or_observation"] }
    },
    "EXECUTE": {
      "contract_version": "1.0.0",
      "purpose": "Commit the selected provider action.",
      "required_inputs": ["selected_option", "provider_ref", "policy_decision_ref"],
      "outputs": ["execution_receipt"],
      "risk": "medium",
      "approval": "depends_on_policy",
      "context_requirements": ["permissions", "policy_refs"],
      "strategy_support": ["fastest", "most_trusted", "cheapest", "policy_required"],
      "expected_observations": ["execution.committed"],
      "verification_rules": { "required": true, "evidence": ["receipt_or_observation"] }
    },
    "OBSERVE": {
      "contract_version": "1.0.0",
      "purpose": "Read external state after or during execution.",
      "required_inputs": ["goal_state_ref", "execution_receipt"],
      "outputs": ["observation_event"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["world_state_refs"],
      "strategy_support": ["fastest", "most_trusted", "privacy_first"],
      "expected_observations": ["world_state.observed"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "RECONCILE": {
      "contract_version": "1.0.0",
      "purpose": "Compare observed world state against the goal and choose the next action.",
      "required_inputs": ["goal_plan_ref", "world_state_ref", "policy_refs"],
      "outputs": ["reconciliation_decision"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["world_state_refs", "policy_refs"],
      "strategy_support": ["most_trusted", "policy_required"],
      "expected_observations": ["reconciliation.decided"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "RECOVER": {
      "contract_version": "1.0.0",
      "purpose": "Execute a recovery strategy after partial failure.",
      "required_inputs": ["failure_state_ref", "recovery_policy_ref"],
      "outputs": ["recovery_receipt"],
      "risk": "medium",
      "approval": "depends_on_policy",
      "context_requirements": ["policy_refs", "execution_memory"],
      "strategy_support": ["most_trusted", "fastest", "policy_required"],
      "expected_observations": ["recovery.attempted"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "TRACK": {
      "contract_version": "1.0.0",
      "purpose": "Monitor progress after execution.",
      "required_inputs": ["execution_receipt"],
      "outputs": ["status_snapshot"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["world_state_refs"],
      "strategy_support": ["fastest", "most_trusted"],
      "expected_observations": ["status.updated"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "VERIFY": {
      "contract_version": "1.0.0",
      "purpose": "Confirm real-world completion.",
      "required_inputs": ["expected_outcome", "evidence_refs"],
      "outputs": ["verification_report"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["world_state_refs"],
      "strategy_support": ["most_trusted", "policy_required"],
      "expected_observations": ["verification.completed"],
      "verification_rules": { "required": true, "evidence": ["receipt_or_observation"] }
    },
    "SUSPEND": {
      "contract_version": "1.0.0",
      "purpose": "Pause a goal until time, event, provider state, or user action.",
      "required_inputs": ["goal_state_ref", "wake_condition"],
      "outputs": ["suspension_receipt"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["goal_state_ref"],
      "strategy_support": ["policy_required", "user_preferred"],
      "expected_observations": ["goal.suspended"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "RESUME": {
      "contract_version": "1.0.0",
      "purpose": "Continue a suspended goal.",
      "required_inputs": ["suspended_goal_ref", "wake_event"],
      "outputs": ["resumed_goal_state"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["goal_state_ref"],
      "strategy_support": ["fastest", "policy_required"],
      "expected_observations": ["goal.resumed"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "CANCEL": {
      "contract_version": "1.0.0",
      "purpose": "Reverse or stop a pending action.",
      "required_inputs": ["execution_receipt", "cancellation_policy_ref"],
      "outputs": ["cancellation_receipt"],
      "risk": "high",
      "approval": "user",
      "context_requirements": ["identity", "policy_refs"],
      "strategy_support": ["most_trusted", "policy_required"],
      "expected_observations": ["cancellation.completed"],
      "verification_rules": { "required": true, "evidence": ["receipt_or_observation"] }
    },
    "COMMUNICATE": {
      "contract_version": "1.0.0",
      "purpose": "Send or receive messages.",
      "required_inputs": ["message_schema", "recipient_refs", "channel_ref"],
      "outputs": ["communication_receipt"],
      "risk": "medium",
      "approval": "depends_on_policy",
      "context_requirements": ["identity", "permissions", "policy_refs"],
      "strategy_support": ["fastest", "most_trusted", "privacy_first", "policy_required"],
      "expected_observations": ["communication.sent_or_received"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "SCHEDULE": {
      "contract_version": "1.0.0",
      "purpose": "Create or modify time-bound commitments.",
      "required_inputs": ["time_ref", "participant_refs", "resource_refs"],
      "outputs": ["schedule_receipt"],
      "risk": "medium",
      "approval": "user_when_external",
      "context_requirements": ["permissions", "policy_refs"],
      "strategy_support": ["fastest", "user_preferred", "policy_required"],
      "expected_observations": ["schedule.updated"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "STORE": {
      "contract_version": "1.0.0",
      "purpose": "Persist artifacts or records.",
      "required_inputs": ["artifact_ref", "destination_ref", "retention_policy_ref"],
      "outputs": ["storage_receipt"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["permissions", "policy_refs"],
      "strategy_support": ["fastest", "privacy_first", "policy_required"],
      "expected_observations": ["artifact.stored"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "NOTIFY": {
      "contract_version": "1.0.0",
      "purpose": "Inform the user or another actor.",
      "required_inputs": ["recipient_ref", "message_schema", "channel_ref"],
      "outputs": ["notification_receipt"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["permissions"],
      "strategy_support": ["fastest", "most_trusted", "privacy_first"],
      "expected_observations": ["notification.delivered"],
      "verification_rules": { "required": false, "evidence": [] }
    },
    "LEARN": {
      "contract_version": "1.0.0",
      "purpose": "Record validated improvement signals.",
      "required_inputs": ["correction_ref", "evidence_refs", "validation_target_ref"],
      "outputs": ["learning_event"],
      "risk": "low",
      "approval": "none",
      "context_requirements": ["execution_memory"],
      "strategy_support": ["most_trusted", "policy_required"],
      "expected_observations": ["learning.recorded"],
      "verification_rules": { "required": false, "evidence": [] }
    }
  }
}
